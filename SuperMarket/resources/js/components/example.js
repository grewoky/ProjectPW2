import React from 'react';

// Capitalize the component name (PascalCase)
const Example = () => {
    return (
        <div>
            <h1>Hello, React!</h1>
        </div>
    );
};

export default Example;
